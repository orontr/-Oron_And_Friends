@model AppointmentScheduling.ViewModel.AppointmentViewModel
@using AppointmentScheduling.Models
@{
    ViewBag.Title = "AppointmentScheduling";
}

<h2>AppointmentScheduling</h2>

    
    <br />
    <table class="table table-bordered table-responsive table-hover">
        <tr>
            <td>  רופא  </td>
            <td>  תאריך  </td>

        </tr>
        @foreach (Appointment obj in Model.Appointments)
        {
        <tr>
            <td>@obj.DoctorName </td>
            <td>@obj.Date </td>
            <td>
                @Html.ActionLink("בחר תור", "ChooseAppointment", "Patient", new { DoctorName = obj.DoctorName, date=obj.Date }, null)
            </td>
            
        </tr>
        }
    </table>
